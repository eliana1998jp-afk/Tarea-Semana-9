Al ejecutar el comando node server.js, se inicia la aplicación y el servidor comienza a funcionar en el puerto 3000.

En la terminal aparece el mensaje:
"Servidor ejecutándose en puerto 3000".

Cuando se visita la ruta http://localhost:3000 se muestra el mensaje:
"Servidor activo y funcionando".

Cuando se visita la ruta http://localhost:3000/estado se obtiene la siguiente respuesta:

{
"estado": "Servidor funcionando",
"servicio": "API comunitaria"
}

Esto indica que el servidor está activo y respondiendo correctamente a las solicitudes realizadas por los usuarios.
